      <%# いいねアイコン共通view %>
      <%# books/show, users/favorite, users/show(root) %>
      <div>
        <% if current_user.likes.find_by(book_id: book.id) %>
        <%= link_to 'いいねを取り消す', book_like_path(book, current_user.id), method: :delete, remote: true %>
        <% else %>
        <%= link_to 'いいね', book_likes_path(book), method: :post, remote: true %>
        <% end %>
      </div>
